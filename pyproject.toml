[project]
name = "iris-clustering-analysis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scikit-learn",
    "scipy",
    "scipy-stubs~=1.17.1",
    "pandas-stubs~=3.0.3",
]
